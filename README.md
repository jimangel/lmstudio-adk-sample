Demo using ADK with a local model running in lmstudio and a work-around for the limitation that built-in google search can only be called by Gemini models (can use a AgentTool).

```
git clone ...

adk web
```